#!/bin/sh
set -e

case "$1" in
configure)
	cd /usr/lib/distiller-update

	[ -d .venv ] && rm -rf .venv

	export UV_COMPILE_BYTECODE=1
	export UV_NO_CACHE=1
	export UV_LINK_MODE=copy

	uv sync --frozen --no-editable --compile-bytecode || {
		echo "ERROR: Failed to install package dependencies" >&2
		exit 1
	}

	install -d -m 0755 /var/cache/distiller-update
	install -d -m 0755 /var/log/distiller-update

	echo "INFO: distiller-update installed successfully."
	echo "INFO: Start service: sudo systemctl start distiller-update.timer"
	;;

abort-upgrade | abort-remove | abort-deconfigure) ;;

*)
	echo "ERROR: postinst called with unknown argument \`$1'" >&2
	exit 1
	;;
esac

#DEBHELPER#

exit 0
